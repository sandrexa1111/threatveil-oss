'use client';
import Link from 'next/link';
import { useState, type FormEvent } from 'react';
import { ArrowUpRight, ArrowRight, ShieldCheck, GitBranch, Fingerprint, Check, Plus, Minus, Menu, X, FileCheck2, ScanLine } from 'lucide-react';
import { Brand } from './brand';
import { PricingPlans } from './commercial';
import { api } from '@/lib/api';
export function PublicShell({ children }: { children: React.ReactNode }) {
  const [open, setOpen] = useState(false);
  return <div className="public-site"><header className="public-header"><Brand /><button className="mobile-menu icon-button" aria-label="Toggle navigation" aria-expanded={open} onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button><nav aria-label="Main navigation" className={open ? 'public-nav open' : 'public-nav'}><Link href="/product">Product</Link><Link href="/pricing">Pricing</Link><Link href="/security">Security</Link><Link href="/docs">Docs</Link><Link href="/login" className="login-link">Log in <ArrowUpRight size={14} /></Link><Link href="/login" className="button dark">Start free <ArrowUpRight size={16} /></Link></nav></header>{children}<footer className="public-footer"><div><Brand /><p>Assurance for autonomous systems in motion.</p></div><div><Link href="/security">Security & trust</Link><Link href="/docs">Documentation</Link><Link href="/contact">Contact</Link><span>© {new Date().getFullYear()} ThreatVeil</span></div></footer></div>;
}
export function Home() { return <PublicShell><main id="main"><section className="hero"><div className="hero-copy"><div className="eyebrow"><span className="pulse-dot" /> CHANGE ASSURANCE FOR AI AGENTS</div><h1>Your AI agent changed.<br />Which security conclusions<br /><em>are still true?</em></h1><p>Models, prompts, tools, permissions and deployments change. ThreatVeil determines which security conclusions still apply, which no longer do, and what must be re-established before consequential authority continues.</p><div className="hero-actions"><Link href="/login" className="button dark">Start free <ArrowUpRight size={17} /></Link><Link href="/contact" className="text-link">Apply for an Assurance Launch <ArrowRight size={17} /></Link></div><div className="hero-caption"><ShieldCheck size={15} /> Connect your own agent, or explore an example. Any agent with consequential tools.</div></div><div className="hero-visual" aria-label="Illustrative change-assurance lifecycle, not customer results"><div className="visual-topline"><span>ONE CLAIM. THREE CHANGES.</span><span>ILLUSTRATION</span></div><div className="property-note"><div className="eyebrow"><Fingerprint size={14} /> APPROVAL REQUIRED</div><h2>A consequential change must never<br />commit without a recorded approval.</h2><div className="mini-tag">Any agent with consequential tools</div></div><div className="visual-timeline"><div><span className="timeline-node pass-node"><Check size={13} /></span><div><small>01 · CLEARED</small><h3>Claim supported by current evidence</h3><p>Forbidden outcome prevented, useful task still works</p></div><span className="status status-pass">ALLOW</span></div><div><span className="timeline-node pass-node"><Check size={13} /></span><div><small>02 · CHANGED</small><h3>A tool permission moved</h3><p>Authority expanded. The evidence behind that claim no longer applies.</p></div><span className="status status-fail">BLOCK</span></div><div><span className="timeline-node change-node"><GitBranch size={13} /></span><div><small>03 · RE-ESTABLISHED</small><h3>Re-proven against the new state</h3><p>Clearance returns only when both hold again</p></div><span className="status status-pass">ALLOW</span></div></div><div className="visual-bottom"><span className="mini-pulse" /> Each conclusion belongs to one exact system state.</div></div></section><section className="principle-strip"><span>DETECT CHANGE.</span><span className="strip-arrow">↗</span><span>RE-PROVE.</span><span className="strip-arrow">↗</span><span>DECIDE.</span></section><section className="public-section"><div className="section-intro"><div className="eyebrow">WHEN YOUR AGENT CHANGES</div><h2>What changed?<br />What is still true?</h2><p>A passing test belongs to the system it observed. ThreatVeil connects security properties to their proof assumptions, candidate changes, and release evidence.</p></div><div className="three-grid feature-grid">{[{icon:ScanLine,n:'01',title:'Know what became stale',body:'Compare the exact candidate with the evidence scope. Changed dependencies require re-proof. Unknown dependencies remain visible.'},{icon:FileCheck2,n:'02',title:'Re-prove the consequence',body:'Check the actual side effect with qualified observations. Security must hold while the legitimate business task still succeeds.'},{icon:GitBranch,n:'03',title:'Decide with evidence',body:'Inspect ALLOW, WARN, or BLOCK for the exact candidate, with the properties, failures, unknowns, and recorded justification behind it.'}].map(f=><article key={f.n}><div className="feature-top"><f.icon size={25} strokeWidth={1.5}/><span>{f.n}</span></div><h3>{f.title}</h3><p>{f.body}</p></article>)}</div></section><section className="gauntlet-promo"><div><div className="eyebrow">THREATVEIL ASSURANCE LAUNCH</div><h2>One workflow.<br />One confirmed consequence.</h2><p>A paid, bounded engagement on one consequential workflow in one staging environment: three claims, one live source, one qualified observer, the Assurance Gate installed in WARN, a Current Assurance Passport, one confirmed consequence and one restoration cycle.</p></div><div className="promo-details"><div><span>01</span> One workflow, one staging environment</div><div><span>02</span> Three claims and one qualified observer</div><div><span>03</span> Gate in WARN, a passport, one confirmed consequence</div><Link className="button cream" href="/contact">Apply for an Assurance Launch <ArrowUpRight size={17}/></Link></div></section><section className="closing-section"><p>BEFORE YOUR NEXT MODEL, PROMPT, OR TOOL CHANGE</p><h2>Does current evidence still justify this system’s authority to act?</h2><Link className="button dark" href="/login">Start free <ArrowUpRight size={17}/></Link><Link className="text-link" href="/contact">Or apply for an Assurance Launch <ArrowRight size={17}/></Link></section></main></PublicShell>; }
export function Pricing() { return <PublicShell><main id="main"><section className="page-intro"><div className="eyebrow">PRICED AROUND PROTECTED SYSTEMS</div><h1>Start with one system.<br />Grow with its authority.</h1><p>Experience change assurance on Free, including a non-blocking control point. Pro adds the enforcing CI gate. Business is where ThreatVeil can participate in production enforcement for consequential systems.</p><Link href="/login" className="button dark">Start free <ArrowUpRight size={17}/></Link></section><PricingPlans/><section className="public-section two-grid"><article className="simple-card"><div className="eyebrow">ASSISTED INTEGRITY LAUNCH</div><h2>A defined boundary.<br />A useful first result.</h2><p>For teams ready to connect a consequential system, agree 3–5 meaningful properties, qualified observations, a legitimate task, and an initial WARN workflow. Timing and price depend on prepared access and accepted scope.</p><Link href="/contact" className="text-link">Scope an Integrity Launch <ArrowRight size={16}/></Link></article><article className="simple-card"><div className="eyebrow">ENTERPRISE</div><h2>Your operating requirements.</h2><p>Define system allowances, environments, retention, verification budgets, supported connectors, deployment, support, and professional services through one contract. Technical activation remains subject to deployment acceptance.</p><Link href="/contact" className="text-link">Discuss your requirements <ArrowRight size={16}/></Link></article></section></main></PublicShell>; }
export function Contact() { const [status,setStatus]=useState(''); const [busy,setBusy]=useState(false); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setStatus('');const form=e.currentTarget;const f=new FormData(form);try{const r=await api('/leads',{method:'POST',data:{...Object.fromEntries(f),consent:f.get('consent')==='on'}});setStatus(r.message?String(r.message):r.routing==='awaiting_crm_configuration'?'Your request was recorded. Follow-up delivery is pending configuration.':'Your request was received and queued for follow-up.');form.reset();}catch(error){setStatus(error instanceof Error?error.message:'Unable to send request.');}finally{setBusy(false)}} return <PublicShell><main id="main" className="contact-layout"><div><div className="eyebrow">ONE SYSTEM. AN INSTALLED RELEASE WORKFLOW.</div><h1>Scope your<br />Integrity Launch.</h1><p>Tell us what your agent can change, how you release it, and which security conclusion you need to keep current. We’ll define the properties, staging boundary, authoritative observations, and release integration together.</p><div className="contact-points"><span><Check/>A bounded launch with a named engineering owner</span><span><Check/>Describe access without sharing credentials</span><span><Check/>Agree scope and authorization before execution</span></div></div><form onSubmit={submit} className="contact-form"><label>Name<input name="name" required maxLength={120} autoComplete="name"/></label><label>Work email<input name="email" type="email" required maxLength={254} autoComplete="email"/></label><label>Company<input name="company" required maxLength={160} autoComplete="organization"/></label><label>What does your agent do?<textarea name="message" required rows={5} maxLength={2000} placeholder="Tools, data access, consequential actions, and your next planned change…"/></label><label className="checkbox-label"><input name="consent" type="checkbox" required/>I agree to be contacted about this request.</label><button className="button dark" disabled={busy}>{busy?'Sending…':'Request an Integrity Launch'}<ArrowUpRight size={16}/></button>{status&&<p className="form-message" role="status">{status}</p>}</form></main></PublicShell> }
const content:Record<string,{eyebrow:string,title:string,description:string,sections:{title:string,body:string}[]}>= {
product:{eyebrow:'CHANGE ASSURANCE FOR AI AGENTS',title:'What can your agent do?\nIs it still cleared to act?',description:'Models, prompts, tools, permissions and deployments change. ThreatVeil shows what your autonomous system can do, what changed, which security conclusions still hold, and whether it is still cleared to act.',sections:[{title:'What it can do',body:'A living system map and authority map: every consequential action, the tools and identities behind it, the claims that govern it, and whether current evidence still supports it. A tool a source reports is never treated as a granted permission.'},{title:'What changed',body:'A tool permission changed outside any repository. ThreatVeil classifies the authority change as expanded, contracted, equivalent or unknown, and shows exactly which claims depend on it and which evidence relied on the previous state.'},{title:'What still holds',body:'Security evidence has a shelf life. Each claim says whether its evidence still describes the system running now. Unaffected claims stay supported; unknown stays unknown.'},{title:'Re-establish assurance',body:'Clearance returns only when the forbidden outcome is prevented and the legitimate task still works. Blocking useful work is not a fix.'},{title:'Assurance Gate for machines',body:'One read-only answer your CI, deployment or agent runtime can consume: CURRENT, SUPERSEDED, REASSESS, EXPIRED, REVOKED or UNKNOWN. UNKNOWN never means cleared; your policy decides availability.'},{title:'Assurance Passport for your customers',body:'A signed, bounded assurance case your enterprise buyer can verify without an account. It stays authentic after your system changes, and its current status tells them when it no longer describes today.'}]},
security:{eyebrow:'SECURITY & TRUST',title:'Trust the scope.\nInspect the evidence.',description:'Clear boundaries and honest limitations are part of the product. Security claims must describe the system that has actually been tested.',sections:[{title:'Authorized and bounded execution',body:'Targets require authorization, permitted operations, and expiry. Execution is bounded by plan, quota, time, and network restrictions. The launch product does not actuate physical systems.'},{title:'Organization separation',body:'The control plane enforces organization membership, roles, tenant-safe relationships, and database row-level security. Runners receive scoped execution access.'},{title:'Evidence and data handling',body:'The control plane applies structured redaction and renders evidence as escaped content. Redaction is not a guarantee that arbitrary customer payloads contain no sensitive data. Minimize submitted data, declare retention, and review permitted exports. Raw customer evidence remains organization-scoped.'},{title:'Scope, not absolute promises',body:'PASS describes observed behavior under a declared verification scope. It is not universal proof of safety. Self-report, independent observations, missing telemetry, and fixture limits remain distinguishable.'},{title:'Architecture and credentials',body:'The API coordinates properties, runs, evidence, and release policy. The runner/broker design uses short-lived, run-scoped access and credential references. Dedicated cloud execution, managed encryption, and network boundaries require deployment-specific acceptance before use.'},{title:'Retention and deletion',body:'Raw evidence retention and expiry are separate from the historical decision record. Confirm retention, residency, export, deletion, and backup handling in the agreed deployment scope. No cross-customer training rights are implied by submitting evidence.'},{title:'Service providers',body:'The repository supports Google Cloud for hosting and identity, Stripe for billing, Resend for email, HubSpot for CRM, and configured model providers. Which services process your data depends on the deployed configuration; request the deployment-specific subprocessor list and terms before onboarding.'},{title:'Vulnerability disclosure and incidents',body:'Use the contact form to request a private security-reporting channel; do not include credentials or raw evidence. Incident severity, response ownership, customer notification, and recovery procedures must be agreed for the deployed service. No response-time or availability SLA is claimed here.'},{title:'Deployment status',body:'Local development and synthetic testing are distinct from production assurance. Live cloud, payment, and provider integrations require configured access and service-specific verification. No certification is claimed.'}]},
docs:{eyebrow:'GETTING STARTED',title:'Connect.\nWatch.\nShip with confidence.',description:'Three steps to a real answer about your own system, then a deeper setup when you want verified assurance.',sections:[{title:'1. Connect your system',body:'Name the workflow you want to protect and import its declared definition — Claude Code settings, an .mcp.json, a subagent file, langgraph.json, CrewAI, or a ThreatVeil manifest. Secrets are never read: only names and digests. A first import establishes what ThreatVeil has seen; it invalidates nothing.'},{title:'2. Define what must stay true',body:'State one security claim in business language: the action it governs, the forbidden outcome, and the legitimate work that must keep succeeding. It stays NEEDS EVIDENCE until an approved executable check with a qualified observer is bound to it — ThreatVeil never reports a defined claim as supported.'},{title:'3. Check a proposed change',body:'Paste the configuration you are about to ship and ask what it would break. You get the affected claims, the unaffected ones, and the evidence that would need re-establishing. The check is read-only: your current assurance is untouched.'},{title:'4. Establish verified assurance',body:'When you want a current, evidence-backed answer: connect an evidence source, qualify an observer against the harness, establish a baseline, then watch for change. A tool response alone cannot establish a committed business effect.'},{title:'5. Restore when something changes',body:'When a change affects a claim, ThreatVeil says which evidence stopped applying and what must be re-established. Assurance returns only when the forbidden outcome is prevented and the legitimate task still succeeds.'},{title:'6. Share the current answer',body:'Issue a signed Current Assurance Passport for a customer, or point your pipeline at the Assurance Gate. Authenticity and current status are separate checks: a passport stays authentic after the system changes, and its status check says it is no longer current.'}]}
};
export function InformationPage({kind}:{kind:string}) {const c=content[kind]||content.product;return <PublicShell><main id="main"><section className="page-intro"><div className="eyebrow">{c.eyebrow}</div><h1 className="pre-line">{c.title}</h1><p>{c.description}</p></section><section className="information-grid">{c.sections.map((s,i)=><article key={s.title}><span className="section-index">{String(i+1).padStart(2,'0')}</span><div><h2>{s.title}</h2><p>{s.body}</p></div></article>)}</section><section className="closing-section"><h2>Start with your real workflow.</h2><Link href="/login" className="button dark">Start free <ArrowUpRight size={17}/></Link><Link href="/contact" className="text-link">Or discuss an Integrity Launch <ArrowRight size={17}/></Link></section></main></PublicShell>}
